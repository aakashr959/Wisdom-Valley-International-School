document.addEventListener('DOMContentLoaded', () => {
    // Initialize AOS (Animate On Scroll) library for scroll animations
    AOS.init({
        duration: 1000, // Animation duration in milliseconds
        once: true      // Whether animation should only happen once
    });

    // You can add any specific JavaScript for the about page here
    // For example, if you had a dynamic counter, a carousel, or accordions.
    // For now, only AOS initialization is explicitly needed.
});
